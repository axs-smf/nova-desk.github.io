# Deployment

Keep the project structure intact. For local preview run `python -m http.server 8080` from the project root. The catalog and imagery are local, so product pages do not depend on third-party image hosts.
